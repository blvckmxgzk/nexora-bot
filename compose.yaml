services:
  nexora:
    build:
      context: .

    env_file:
      - .env

    environment:
      NODE_ENV: production
      API_HOST: 0.0.0.0

    ports:
      - "${API_PORT:-3000}:${API_PORT:-3000}"

    restart:
      unless-stopped

    init:
      true
